# Haze4K v5 CHD-RM v4a-A1X Accessibility v3

Date: 2026-07-16

Status: PLANNED

## Identity

- Route id: haze4k_v5_chd_rm_v4a_a1x_accessibility_v3_20260716
- Question: Can the adequate exact-half action interface be recovered from inference-eligible tensors by a multiscale-global readout, after engineering integrity and a same-development 2x2 mechanism screen?
- Rules commit: 2a4a0cbad1be2a4dcf7bcb11f8a89ad01eb35339
- Source branch/commit: immutable github/codex/haze4k-official-arch-anchor@3b4da35440c8c26a7d1bcaf1daf342e11d9a3898
- Route branch: codex/haze4k-v5-v4a-a1x-accessibility-v3-20260716
- Locked test/canary policy: blocked at S0, D0, and formal; no name, image, target, prediction, metric, or outcome access.

## Scientific Contract

- Population and analysis/grouping unit: S0 uses only the independent 32-name A1R engineering-debug manifest; D0 would use the already consumed A1R fresh512 development population; formal would use all 432 untouched A1X clean-reference groups exactly once only after D0 PASS.
- Intervention or factor contrast and reference: D0 is a frozen 2x2 representation-by-readout audit: A1R context+output versus the five A1X output-side tensors, crossed with A1R local-spatial versus A1X multiscale-global readout; cells share folds, seed, optimizer, epochs, loss, and budget.
- Primary outcome, direction and aggregation: higher safe rendered PSNR, paired by clean-reference image and operator, with the worse D_ref/D_rep operator retained inside each image bootstrap draw.
- Preferred mechanism and strongest competing explanation: multiscale-global aggregation recovers image-conditioned safe direction; the strongest alternative is that any gain is a population prior or unchanged local-readout signal reproduced by shuffled targets.
- Evidence roles and candidate/freeze point: S0 is engineering_debug; D0 is development_screening on already consumed A1R fresh512; the 432-name stage is confirmation. The head, exact-half transport, five inputs, 2x2 factors, folds, seed, optimizer, two epochs for S0, eight epochs for later stages, loss, thresholds, and stop actions are frozen before runtime.
- Primary gate, uncertainty and threshold source: S0 is structural integrity including exact A1C transport equality, strict freeze/load, exact no-op, two native shapes, finite forward/backward, nonzero head gradient, at least 1% image-balanced microfit target-loss reduction, and parameter/MAC limits. D0 later requires gain LCB95 >= +0.020 dB, retention LCB95 >= 0.25, true-minus-shuffle LCB95 >= +0.005 dB, paired global-minus-local LCB95 > 0, and all structural/safety guards; thresholds come from the frozen A1R/A1C handoff, not S0 results.
- `PASS` authorizes: S0 PASS authorizes only A1X_V3_D0_DESIGN_ONLY; D0 PASS may authorize a separately committed one-algorithm formal operation after review.
- `INCONCLUSIVE` authorizes: no automatic continuation; only the action explicitly frozen in the later D0/formal card amendment, never threshold, width, epoch, learning-rate, input-subset, or seed search.
- `FAIL` stops: S0 engineering failure stops this implementation; D0 scientific failure stops this global-head contract before any 432-name access; formal failure stops same-data alternate-head search.

## Implementation Contract

- Exact change and disabled mechanisms: add standalone A1X_ACCESS_Head and S0-only entrypoint; official ConvIR architecture/builders remain untouched. No interface variant, width search, input ablation, action-grid change, policy, candidate, checkpoint promotion, or sealed evaluation is enabled.
- Checkpoint/load/init/freeze contract: official Haze4K base, frozen control, frozen D_ref/D_rep artifacts, and frozen current Delta-u state use pinned paths/SHA-256; all historical parameter names are strict-loaded and frozen; only A1X_ACCESS_Head is trainable; its final projection is exactly zero initialized.
- Input whitelist and prohibited inputs: forward inputs are exact-half hazy RGB, frozen base RGB, old .125 RGB, old .25 RGB, and current Delta-u. Clean RGB, target Delta-u, safe-grid winner, action amplitude, outer outcomes, confirmation names/outcomes, canary, and locked test are prohibited forward inputs.
- Dataset/split/preprocessing/metric identities: Haze4K train paths are fixed; S0 may load images only for the explicit debug32 list derived from A1R fresh indices 256:288. A1C transport is 400x400 to 208x208 and 480x640 to 240x320, bilinear align_corners=False and antialias=False, low difference within +/-2B, soft support, upsample, and final channelwise clamp.
- Matched baseline and budget: S0 has no scientific baseline and only a two-epoch engineering microfit. D0 later uses the identical A1R local-spatial cell and true/hash-deranged shuffle controls under matched four-fold budget.
- Resource/cost limits or descriptive-only rationale: S0 hard limit 20 minutes, added parameters <=300000, largest exact-half MACs <=600000000, at least 12000 MiB free GPU before launch; peak memory and wall time are recorded. No scientific claim follows from cost.
- Runner and required assets: experience_docx/tools/run_chd_rm_v4a_a1x_accessibility_v3_s0.sh; S0 entrypoint, debug32 manifest, parent128 state-identity split, asset manifest, A1C reference source, and generic run telemetry.

## Operations And Evidence

- S0 r1 closeout: `FAILED_ENGINEERING` before `preflight_pass`; the pinned A1C
  reference checkout path was absent on `convir-4090`. No runtime log or model
  summary was created, and confirmation/canary/locked-test flags remained
  false. This is an asset-packaging failure, not an S0 gate result.
- Frozen repair boundary: vendor only the exact A1C `endpoint` reference needed
  for bit-exact transport comparison into the route commit, preserve its
  upstream commit/source hash provenance, and use a fresh `a1x-v3-s0-r2`
  output. No data, model, loss, threshold, epoch, LR, width, or gate changes are
  authorized by this repair.

- First operation: A1X_V3_S0_R2. The r1 failure is archived evidence, not an authorization predecessor.
- Expected wall time and monitor profile: at most 20 minutes; short profile; 60-second metadata-only heartbeat, 300-second stale threshold, one expected-end observation.
- Complete-unit resume policy: none for S0; an interrupted S0 uses a new output id after one engineering review. Future formal may resume only complete hash-matching fold/cell units after separate authorization.
- Cloud workspace/run/output/status/closeout: fresh route workspace; run root /sda/home/wangyuxin/ConvIR-B/runs/haze4k_v5_chd_rm_v4a_a1x_accessibility_v3_20260716; new output a1x-v3-s0-r1; status.txt, heartbeat.json, runtime.log, and a1x_v3_s0_closeout.json.
- Compact Git evidence and cloud-only raw artifacts: Git retains route card, schema-v4 operation, manifests, compact S0 summary/closeout/README. Runtime log, tensors, arrays, images, caches, and any raw rows remain cloud-only.

| Operation | Evidence role | Fixed scope | Allowed terminal action |
| --- | --- | --- | --- |
| A1X_V3_S0 | engineering_debug | explicit debug32 x two operators, two-epoch microfit | D0 design only on PASS; otherwise stop |

## Planned Later Stages

D0 will not be added to route_operations.json until S0 has a committed typed PASS. Its 2x2 audit will use all A1R fresh512 development groups, four folds, true and hash-deranged shuffle targets, image-balanced training, paired image bootstrap, and the five fixed promotion requirements above. D0 FAIL stops before confirmation data use.

Formal will not be implemented or listed until D0 has a committed typed PASS. It will freeze exactly one algorithm/code hash, all 432 groups, four folds, true and hash-deranged shuffle, complete-unit recovery only, no interim fold outcome exposure, fold/native-shape stability reporting, and one terminal PASS/FAIL/INCONCLUSIVE interpretation. Even PASS authorizes only R3 review of direction accessibility under the privileged amplitude selector.
