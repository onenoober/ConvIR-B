# R16 S3 Domain-Matched Action Ceiling

Date: 2026-07-20

Status: PLANNED

## Identity

- Route id: \`haze4k_v5_r16_s3_domain_matched_action_ceiling_20260720\`.
- Question: Does one frozen NH-HAZE domain-matched endpoint action system restore a material, spatially specific and tail-safe privileged regional capacity ceiling under the unchanged PSNR/fixed-8x8 measurement contract?
- Rules commit: \`github/main@7080dc2c44006f5b62c6a3d302e025c2fb046778\`.
- Source branch/commit: R15-S0E terminal branch \`codex/haze4k-v5-r15-s0e-same-action-population-precision-20260720@f937ced54\`; direct authorizing launch closeout commit \`b1cc5fc7bc9919394682b278dd864170217a089b\`.
- Route branch: \`codex/haze4k-v5-r16-s3-domain-matched-action-ceiling-20260720\`.
- Locked test/canary policy: confirmation, canary, locked test and NH-HAZE ids 51-55 are prohibited and remain sealed.
- Operation: \`R16_S3_DOMAIN_MATCHED_ACTION_CEILING\`.
- Direct authority: R15-S0E
  \`COMPLETED_GATE_FAIL / R15_S0E_REAL_ACTION_HEADROOM_FAIL_STRATEGIC_RESET /
  R15_S3_REFORMULATION_ONLY\` at route commit \`b1cc5fc7\`.
- Rules anchor: \`github/main@7080dc2c44006f5b62c6a3d302e025c2fb046778\`.
- Confirmation, canary, locked test and NH-HAZE ids 51-55 are prohibited.

## Scientific Contract

- Population and analysis/grouping unit: Exactly NH-HAZE ids 01-50 in the \`development_screening\` control-plane role and the scientifically narrower checkpoint-training-overlap privileged-capacity role; one image is the only independent group, and tiles, actions, execution replicas and bootstrap draws are nested.
- Intervention or factor contrast and reference: Change only the compound \`action_system_identity\` from the failed Haze4K baseline/action source to frozen A0_NH plus or minus the full WDMamba_NH endpoint residual; no-op is A0_NH and all PSNR/8x8/control definitions stay fixed.
- Primary outcome, direction and aggregation: Larger worse-replica regional PSNR gain, region-minus-safe-global and region-minus-spatial-shuffle are better; use 4,000 paired image-bootstrap draws plus two frozen 25-image SHA audit strata, and report CVaR5, severe/hard, mixed and bidirectional coverage.
- Preferred mechanism and strongest competing explanation: Preferred H-A is cross-domain action-source collapse; strongest H-B is that PSNR/fixed-8x8/symmetric-three-action semantics remain unsuitable even with a domain-matched action system.
- Evidence roles and candidate/freeze point: R15-S0E \`development_screening\` evidence is the sole prior authority; R16 is a training-overlap privileged capacity ceiling, and population, endpoint checkpoints/sources, actions, controls, metrics, thresholds, bootstrap, strata and terminal mapping freeze at the launch commit.
- Primary gate, uncertainty and threshold source: Inherit R10/R15-S0E LCB95 materiality +0.020/+0.005/+0.005 dB, CVaR5 -0.005 dB, zero severe/hard, mixed/bidirectional 0.25/0.10 and CI-half-width 0.020 dB; a negative stratum requires its UCB below an inherited margin, not point failure alone.
- `PASS` authorizes: Only \`R16_INDEPENDENT_REAL_DEVELOPMENT_ACTION_QUALIFICATION_CONTRACT_ONLY\`; no training, selector/critic reopening, protected access, deployment or generalization claim.
- `INCONCLUSIVE` authorizes: Only \`R16_PRIVILEGED_CAPACITY_EVIDENCE_COMPLETION_ONLY\`; no threshold change, pseudoreplication or model work.
- `FAIL` stops: Close the \`PSNR + fixed 8x8 + symmetric domain-matched endpoint residual\` capacity route and authorize only \`R16_REGION_TARGET_SUPERVISION_REFORMULATION_ONLY\`; no alpha/amplitude rescue.

## Implementation Contract

- Exact change and disabled mechanisms: Replace only the compound baseline/action-source identity, run no training or fitting, and disable alpha search, selector, critic, feature fitting, checkpoint/sample/threshold selection and any use of v2.8 deleted results or v2.9 ids 51-55 outcomes.
- Checkpoint/load/init/freeze contract: Strictly load official ConvIR-B NHR and WDMamba NH checkpoints at their declared SHA-256 identities, construct the frozen official architectures, set eval/no-grad and introduce no initialization or partial-load rule.
- Input whitelist and prohibited inputs: Permit only exact NH-HAZE hazy/GT paths for ids 01-50, the typed population manifest, R15-S0E closeout, frozen source/checkpoint assets and R10 measurement source; prohibit ids 51-55, confirmation, canary, locked test, historical result tables and undeclared files.
- Dataset/split/preprocessing/metric identities: Native paired RGB 1600x1200 PNG, no resize, legacy endpoint padding, [0,1] clamp, fixed floor-boundary 8x8 RGB SSE and PSNR; SHA-balanced audit strata contain 25/25 images.
- Matched baseline and budget: Region/global/shuffle share identical images, endpoint renders, pixels and action histograms; two deterministic execution replicas must be identical and do not increase sample size; use 50 groups, 16 shuffles and 12,000 total bootstrap draws.
- Resource/cost limits or descriptive-only rationale: One GPU, expected 3,600 seconds, 7,200-second timeout and no resume; the CPU contract constructs the exact models on a 64x64 protected-data-free tensor and executes the full 50-group/12,000-draw measurement work class.
- Runner and required assets: Use the unchanged generic runner, typed R16 runtime spec/asset manifest, immutable official anchor, two SHA-bound checkpoints, five individually SHA-bound WDMamba sources, R10 source and R15-S0E closeout.

## Unique Scientific Question

On the NH-HAZE training-overlap population ids 01-50, does replacing the
cross-domain Haze4K baseline/action source with the frozen domain-matched
ConvIR-B-NH/WDMamba-NH endpoint pair restore a material, spatially specific and
tail-safe privileged regional action ceiling under the otherwise unchanged
PSNR, fixed 8x8, no-op/positive/negative, safe-global and spatial-shuffle
contract?

This operation distinguishes two upstream explanations. H-A says R15-S0E
collapsed mainly because its Haze4K action system did not transport to NH-HAZE;
a domain-matched endpoint residual should restore privileged regional headroom.
H-B says PSNR plus fixed 8x8 plus symmetric three-action semantics is itself
the limiting definition; even a domain-matched endpoint pair will not restore
the old materiality. It does not test deployable ranking, selector design,
generalization or external validity.

## Frozen Intervention And Population

- The only scientific variable is the compound \`action_system_identity\`:
  domain-matched NH-HAZE endpoint baseline/residual replaces the R15-S0E
  Haze4K baseline/action source. This compound variable cannot distinguish the
  baseline contribution from the residual-source contribution.
- Frozen baseline/no-op: official ConvIR-B \`build_net("base", "NHR",
  "original")\` with \`nhhaze-base.pkl\`.
- Frozen positive action: the full endpoint residual
  \`WDMamba_NH - A0_NH\`; positive render is exactly \`WDMamba_NH\` after the
  common [0,1] endpoint clamp.
- Frozen negative action/render: the strict opposite residual
  \`clamp(A0_NH - (WDMamba_NH - A0_NH), 0, 1)\`.
- No alpha, scale, checkpoint, seed, sample, threshold or candidate search is
  permitted. Historical alpha=0.375 evidence is not an input.
- Population: exactly NH-HAZE ids 01-50. These images overlap the checkpoint
  training role, so they are a \`privileged_capacity_ceiling\` population only.
  An image is the only independent statistical/generalization unit. Tiles,
  actions, inference replicas and bootstrap draws are nested, not samples.
- Ids 51-55 must not be listed, opened, hashed, inferred or scored. Their old
  v2.9 results are not used. The deleted v2.8 all-55 result must not be restored
  or cited.

## Frozen Measurement Contract

- All 50 hazy images are first processed without GT. The action tensors and
  exact tensor hashes are sealed for every image. A second deterministic pass
  regenerates the endpoints and must reproduce all hashes before any GT is
  opened. GT is then opened one image at a time only for scoring.
- \`D_ref\` and \`D_rep\` are two deterministic execution/replay lanes of the
  same frozen endpoints. They are an implementation-stability control, not two
  independent scientific operators and not extra sample size. Their action
  tensors must match exactly.
- Preserve the R10 floor-boundary fixed 8x8 RGB-SSE partition, local action
  materiality, safe whole-image oracle, 16 action-histogram/pixel-area-preserving
  spatial shuffles, image-grouped bootstrap, severe/hard definitions and
  mixed/bidirectional summaries.
- Primary estimands, using the worse replay lane inside every image-bootstrap
  draw: regional-oracle gain over no-op, region-minus-safe-global and
  region-minus-spatial-shuffle. Safety is region-minus-global CVaR5 plus severe
  and hard image counts. Coverage is active area plus mixed and bidirectional
  image fractions.
- Four thousand paired image-bootstrap draws use PCG64 seed 3407. Two
  preregistered SHA-balanced 25/25 audit strata use 4,000 draws with seeds 3407
  and 3408. Strata are descriptive consistency checks unless their UCB is
  decisively below an old materiality margin; point failure alone cannot create
  a FAIL.

## Gates And Terminal Mapping

The thresholds are inherited unchanged from R10/R15-S0E:

- regional-gain LCB95 >= +0.020 dB;
- region-minus-global and region-minus-shuffle LCB95 >= +0.005 dB each;
- region-minus-global CVaR5 LCB95 >= -0.005 dB;
- zero region severe (<= -0.2 dB) and zero hard (<= -0.5 dB) images;
- mixed fraction LCB95 >= 0.25 and bidirectional fraction LCB95 >= 0.10;
- both SHA strata meet the three materiality point thresholds and both replay
  lanes are directionally positive;
- regional-gain CI half-width <= 0.020 dB.

\`PASS\` requires every structural and scientific gate. It proves only that a
domain-matched, training-overlap privileged action ceiling exists. It authorizes
\`R16_INDEPENDENT_REAL_DEVELOPMENT_ACTION_QUALIFICATION_CONTRACT_ONLY\`; it does
not authorize training, selector/critic reopening, confirmation or deployment.

\`FAIL\` requires a decisive scientific contradiction: any pooled or stratum
UCB below an inherited materiality threshold, any severe/hard image, a tail UCB
below -0.005 dB, or a mixed/bidirectional UCB below its margin. It closes the
current \`PSNR + fixed 8x8 + symmetric domain-matched endpoint residual\`
capacity route and authorizes only
\`R16_REGION_TARGET_SUPERVISION_REFORMULATION_ONLY\`. It prohibits changing
alpha/action amplitude to rescue this contract.

\`INCONCLUSIVE\` applies only when valid complete evidence overlaps a gate or
the CI half-width exceeds 0.020 dB without a decisive FAIL. It authorizes only
\`R16_PRIVILEGED_CAPACITY_EVIDENCE_COMPLETION_ONLY\`; no pseudoreplication,
threshold change or model work is allowed.

## Implementation And Integrity

- No training, fitting, checkpoint selection or sample exclusion occurs.
- ConvIR-B code comes from the clean immutable official architecture anchor at
  commit \`3b4da35440c8c26a7d1bcaf1daf342e11d9a3898\` and checkpoint SHA-256
  \`aab6a72613781900a23c3922ad2dd60f6b0d563018e33ae75162bcf3338f5bac\`.
- WDMamba uses the frozen NH checkpoint SHA-256
  \`e097524f466b24f32843867911f9cbd47be8d51e61e5e345f8a27c22c73d5c5a\`,
  \`WaveMamba(... n_l_blocks=[1,2,2,4] ...)\` and \`DENet(3,4)\`. The five
  dynamically loaded project source files are separately SHA-bound in the
  typed asset manifest because that source directory is not a Git checkout.
- The CPU contract exercises the exact model construction and inference path on
  a protected-data-free tensor, verifies deterministic finite endpoints and
  action symmetry, then exercises the full 50-group/12,000-draw measurement
  work class. The run uses one GPU, expected 3,600 seconds, timeout 7,200
  seconds, and no resume.
- Any source, checkpoint, population, role, hash replay, GT-ordering, finite,
  shape, tile-partition or protected-access mismatch is an engineering or
  integrity failure. It cannot be converted to a scientific FAIL and does not
  amend R15-S0E.

## Operations And Evidence

- First operation: R16_S3_DOMAIN_MATCHED_ACTION_CEILING
- Expected wall time and monitor profile: 3,600 seconds with the \`standard\` profile.
- Complete-unit resume policy: \`none\`; recovery, if engineering-authorized, must use a new output id under the same scientific contract.
- Cloud workspace/run/output/status/closeout: MCP-owned fresh workspace on convir-4090; output \`r16-s3-domain-matched-action-ceiling-r2\`; generic status/heartbeat/runtime log; closeout \`r16_s3_domain_matched_action_ceiling_closeout.json\`.
- Compact Git evidence and cloud-only raw artifacts: Git retains the card, typed closeout and eight declared compact identity/endpoint/policy/bootstrap/strata/gate/resource/conclusion files; per-image rows, action hashes, tensors, images, weights and logs remain cloud-only.

| Operation | Evidence role/scope | Gate | Pass authorizes |
| --- | --- | --- | --- |
| \`R16_S3_DOMAIN_MATCHED_ACTION_CEILING\` | training-overlap privileged capacity ceiling | frozen materiality, specificity, safety, coverage and precision | independent-real-development action qualification contract only |

- Same-contract engineering repair: r1 failed before workload because the 64x64 CPU production fixture shrank below ConvIR-B's fixed reflect-padding support; r2 changes only that protected-data-free contract fixture to 256x256 and uses a new output identity.
- Output id: \`r16-s3-domain-matched-action-ceiling-r2\`.
- Generic runner: unchanged
  \`experience_docx/tools/run_route_operation.sh\`.
- Runtime spec:
  \`experience_docx/route_runtime_specs/R16_S3_DOMAIN_MATCHED_ACTION_CEILING.json\`.
- Entrypoint:
  \`experience_docx/tools/r16_s3_domain_matched_action_ceiling.py\`.
- GitHub retains the immutable card, typed closeout, identity/access,
  endpoint/action, policy, bootstrap, strata, gates, resource and scientific
  conclusion files. Per-image/tile rows, action hash manifests, tensors, images,
  weights and raw logs remain cloud-only.
- Required engineering terminal tuple: \`FAILED_ENGINEERING / null / NONE\`.

The card is immutable after launch. R13-R15 terminal decisions remain unchanged.
