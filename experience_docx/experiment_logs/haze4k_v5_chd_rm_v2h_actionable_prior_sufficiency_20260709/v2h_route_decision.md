# v2h Route Decision

Decision: `PLANNED_V2H_ACTIONABLE_PRIOR_SUFFICIENCY_AUDIT`

v2g left D7c as the best deployable prior under the three-state target, but did not prove that D7c-selected regions have enough restoration upper bound to justify future RARM or adapter work. v2h first closes the risk-coverage gap with a non-training calibration diagnostic.

This is a diagnostic continuation, not a new architecture route. It starts from the v2g evidence state and keeps all forbidden flows blocked until v2h A/B closeout writes a new decision.

Forbidden: locked Haze4K test, D2, F5, v3, RARM connection/training, adapter training, and new selective-head/probe expansion.
