# v2.36 Decision Tree

P0:
If full600 same-contract teacher fails, stop before architecture training.
If it passes, P1 architecture identity is authorized; P0B context384 projection
is also authorized as an optional practical-context audit.

P0B:
If context384 free-tensor projection passes, 384 may become the practical
deployment branch after P1. If it fails, do not train a 384 bridge; continue
only with full_image_slice if P0 and later full_image_slice identity gates pass.

P1:
If identity, NoPost, partial-load, or forward contract fails, fix implementation
and do not train. If it passes, P2 generator-vs-free-tensor fit is authorized.

P2:
If one-image or canary32 trainfit fails, treat it as generator capacity,
normalization, or optimization failure. If trainfit passes but tail fails,
redesign preservation/gating. If P2 passes, P3 OOF canary32 is authorized.

P3:
If OOF canary32 fails, do not run canary80. If OOF canary32 passes, P4 canary80
OOF is authorized.

P4:
If canary80 OOF passes, propose a fixed-selection full600 diagnostic. Locked
test remains blocked until a separate locked-confirmation card.
