# C12-0 Route Freeze

Route: `Haze4K v2.4 C12 WD0375 Distillation Feasibility`.

Locked Haze4K is forbidden. The only teacher is train-core WD0375 cache; the held-out C8 `val_regular + val_hard` 600 images are validation only.

Decision after screen: `C12_SCREEN_FAIL_KEEP_WD0375_TEACHER`.
