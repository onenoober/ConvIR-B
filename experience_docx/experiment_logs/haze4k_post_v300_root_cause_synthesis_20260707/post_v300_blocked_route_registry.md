# Post-v3.0 Blocked Route Registry

Blocked continuations:
- `v3.0` wider decoder unfreezing.
- `v3.0` more epochs, folds, samples, or canary80.
- Any locked-test use from v3.0.
- Simple loss-weight, beta/gamma shrink, or anchor-loss rescue.
- A0 residual head continuation from v2.41/v2.42.
- Selector/alpha deployment, bridge, generator, or teacher-delta projection using the failed A0-neighborhood route as authorization.

Reason:
The failure is now classified as `A0_DOMINANCE_ROUTE_FAIL`, not a narrow frozen-head tuning failure.
