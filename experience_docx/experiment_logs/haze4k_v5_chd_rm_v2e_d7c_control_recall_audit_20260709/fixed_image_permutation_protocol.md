# Fixed Image-Level Permutation Control

Frozen D7c predictions are kept on each val_inner image. Target and density masks are reassigned by a fixed image-level derangement. The null distribution uses deterministic seeds and reports empirical p-values against the original D7c candidate.
