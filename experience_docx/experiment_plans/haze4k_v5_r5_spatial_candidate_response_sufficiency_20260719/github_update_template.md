# GitHub Human-Readable Research Update Template

Date: `<YYYY-MM-DD>`

Route or audit: `<route_id / operation_id / run_id>`

## GitHub 初步结论

`<说明 GitHub 正式紧凑证据在云端复核前支持的瓶颈、适用范围和 terminal tuple。>`

## 云端复核发现

`<列出经过身份验证的原始输入、数据角色、截止时间和只读复核范围。>`

云端原始数据提高了机制解释精度，但不改变原实验 terminal decision。

## 正式结果复现

- 正式 terminal tuple：`<state / decision / authorizes>`
- 已复现指标：`<point estimates / calibration / counts>`
- closeout 绑定文件：`<数量及哈希一致性>`
- 材料性差异：`<none 或逐项列出>`

## 支持、收窄与修正

- 得到支持：`<原判断及证据类别 A/B>`
- 被收窄：`<不能再作唯一因果解释的判断>`
- 被修正：`<优先级、范围或措辞变化>`
- 未改变：`<旧路线 PASS/FAIL、terminal decision 和授权>`

## 事后探索性发现

`<明确标记类别 C；列出 abstention、fold、action、tail、risk concordance 等发现，并声明不能改变旧终点或确认新路线。>`

## 工程完整性

`<记录类别 D 的工程/数值问题、是否影响主要科学结果、是否建立独立 integrity incident。不得静默修改旧 closeout。>`

## 更新后的瓶颈

`<用可证伪的正式表述，限定数据、折、算子、动作库、表示和决策合同。>`

## 当前机制假设

| 优先级 | 假设 | 当前状态 | 关键缺口 |
| ---: | --- | --- | --- |
| 1 | `<H1>` | `<优先判别>` | `<missing evidence>` |
| 2 | `<H4>` | `<需要新信息源>` | `<missing evidence>` |
| 3 | `<H2>` | `<次要保留>` | `<missing evidence>` |

## 当前授权

- 允许：`<read-only verification / documentation / exact approved contract action>`
- 运行授权：`<NONE or exact operation id>`
- 数据角色：`<engineering_debug / development_screening / confirmation / sealed_final>`

## 当前禁止事项

- 不得重跑、重调或重建已关闭路线输出。
- 不得访问未获授权的 confirmation、historical A1X outcome、canary 或 locked test。
- 不得以 seed、epoch、学习率、阈值、惩罚权重、宽度、深度或邻近结构搜索冒充新路线。
- 不得在结果后改变统计单位、coverage、severe/hard 定义、比较族或材料性门槛。

## 下一步解锁所需证据

`<列出精确资产身份、独立合同批准、route-ready gate、typed runtime authorization 以及 PASS/FAIL/INCONCLUSIVE 映射。>`

## 下一动作

`<只能写一个：证据补全、离线分析、完整性事件、合同建立、最小机制实验或继续停止。>`

当前 R5 草案的示例写法：

> 已建立独立的空间候选响应充分性路线合同草案，但
> `runtime_authorization=NONE`。当前仅允许合同审阅；不得生成候选响应、训练读出器、执行离线统计或访问保护数据。
