# R5 Spatial Candidate-Response Route-Design Package

Date: 2026-07-19

Status: `DRAFT_NOT_AUTHORIZED`

Runtime authorization: `NONE`

## Decision

The R4B-A1 terminal remains
`COMPLETED_GATE_FAIL / R4B_A1_SETWISE_MECHANISM_FUTILITY_STOP / NONE`.
The cloud audit reproduced the formal result and narrowed the functional
failure to a decision contract that cannot convert candidate-conditioned
signals into material signed utility with safe coverage. It did not identify a
unique upstream cause.

The originally preferred frozen-component replay cannot be executed because
per-action mean/q05 scores and composite confidence were not persisted. Those
fields must not be regenerated under the closed R4B identity. Existing raw
rows have already answered the remaining low-cost descriptive questions about
abstention, fold coverage, positive-only action selection, and candidate-risk
concordance.

The selected next action is therefore governance action D: establish one new
independent route contract and do not run it. The proposed first scientific
operation, if separately authorized later, isolates one new factor: preserving
the spatial layout of candidate-minus-no-op response. This is narrower than
generic spatial modeling and is explicitly distinguished from v4a-A1R and
v4a-A1X.

## Package Index

| File | Purpose | Current evidence role |
| --- | --- | --- |
| route card | complete human preregistration and terminal mapping | draft contract, no scientific evidence |
| `hypothesis_register.json` | ranked mechanisms, counterevidence, missing evidence, route consequences | planning synthesis of A-D evidence |
| `estimand_register.json` | units, contrasts, thresholds, uncertainty, and claim limits | draft preregistration |
| `controls_and_gates.json` | fixed factor cells and PASS/FAIL/INCONCLUSIVE logic | draft preregistration |
| `draft_asset_manifest.json` | required identities and unresolved runtime blockers | documentation only |
| `protected_data_policy.json` | role permissions and prohibited access | binding draft restriction |
| `execution_order.json` | allowed sequence and stop points | governance plan |
| `required_artifact_register.json` | intermediate and terminal artifact ownership | archival plan |
| `candidate_action_information_efficiency.csv` | relative action ranking used to choose the current step | planning calculation |
| `github_update_template.md` | future human-readable project-memory update | template only |

## Evidence Authority

- Formal R4B terminal and compact evidence:
  `github/main@6443ec0da`.
- Identity-verified cloud audit and updated interpretation:
  `github/codex/haze4k-v5-r4b-a1-cloud-evidence-audit-20260718@e8de98ff5`.
- New model source anchor:
  `github/codex/haze4k-official-arch-anchor@3b4da35440c8c26a7d1bcaf1daf342e11d9a3898`.

This planning branch does not replace those authorities and must not add a row
to `EXPERIMENT_TERMINAL_INDEX.jsonl` before a real typed terminal closeout.

## Current Stop Line

Do not create `route_operations.json`, a runtime spec, an entrypoint, a cloud
workspace, an output id, or a plan token. The next permissible event is an
independent contract review that either rejects the draft or explicitly
authorizes materialization of the first route bundle. Materialization still
would not authorize scientific runtime.
