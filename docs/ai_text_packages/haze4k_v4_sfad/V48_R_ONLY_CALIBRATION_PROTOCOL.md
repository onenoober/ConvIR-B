# V48 R-only Calibration Protocol

R-only calibration may estimate or audit a spatial response field, but it must not modify restoration outputs. Passing requires positive correlation with train-derived haze/error proxies and sensible subgroup response. A failed R-only probe blocks SDC-Lite v2 and any skip/FAM modulation using R.
